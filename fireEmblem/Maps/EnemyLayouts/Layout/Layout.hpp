#include "../../../Entities/Units/Enemies/Enemy.hpp"
#include "../../../GridSystem/GridHandler/GridHandler.hpp"

namespace Layouts
{
    class Layout
    {
        /*
        private:
            Enemies::Enemy enemy;
            GridHandlers::GridHandler grid;

        public:
            void FetchGrid(GrindHandlers::GridHandler& grid);
            void SetGrid(GridHandlers::GridHandler& grid);
            void Draw(RenderWindow& window);
            void SpawnEnemy(float positionX, float positionY);
            void SetEnemies();
            */
    };
}